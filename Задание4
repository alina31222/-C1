#include <stdio.h>

int main() {
// Получаем число
  int n;
  printf("Введите целое число: ");
  scanf("%d", &n);
// Битовая операция для проверки числа возведённого в степень
  printf("%d\n", (n && !(n & (n - 1))));
  return 0;
}
